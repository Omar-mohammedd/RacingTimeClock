<UserControl x:Class="RacingTimeClock.Views.RacersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#F5F6F8">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Margin="40,30,40,20">

            <TextBlock Text="Racers"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="#20232A"/>

            <TextBlock Text="Manage registered racers."
                       FontSize="15"
                       Foreground="#777777"
                       Margin="0,5,0,0"/>

        </StackPanel>

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="8"
                Padding="20"
                Margin="40,0,40,15">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>

                    <TextBlock Text="Current Season"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#777777"/>

                    <ComboBox x:Name="SeasonComboBox"
                              Width="180"
                              Height="35"
                              HorizontalAlignment="Left"
                              Margin="0,5,0,0"
                              SelectionChanged="SeasonComboBox_SelectionChanged"/>

                </StackPanel>

                <Button Grid.Column="1"
                        Content="+ ADD RACER"
                        Width="150"
                        Height="45"
                        VerticalAlignment="Center"
                        FontWeight="Bold"
                        Click="AddRacerButton_Click"/>

            </Grid>

        </Border>

        <Grid Grid.Row="2"
              Margin="40,0,40,40">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="White"
                    CornerRadius="8"
                    Padding="15">

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="2.5*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="ID"
                               FontWeight="Bold"
                               Foreground="#777777"/>

                    <TextBlock Grid.Column="1"
                               Text="NAME"
                               FontWeight="Bold"
                               Foreground="#777777"/>

                    <TextBlock Grid.Column="2"
                               Text="YEAR"
                               FontWeight="Bold"
                               Foreground="#777777"/>

                    <TextBlock Grid.Column="3"
                               Text="CATEGORY"
                               FontWeight="Bold"
                               Foreground="#777777"/>

                    <TextBlock Grid.Column="4"
                               Text="STATUS"
                               FontWeight="Bold"
                               Foreground="#777777"/>

                </Grid>

            </Border>

            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          Margin="0,10,0,0">

                <StackPanel x:Name="RacersPanel"/>

            </ScrollViewer>

        </Grid>

    </Grid>

</UserControl>
