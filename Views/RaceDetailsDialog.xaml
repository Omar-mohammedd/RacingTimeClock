<Window x:Class="RacingTimeClock.Views.RaceDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Race Details" Height="580" Width="700"
        WindowStartupLocation="CenterOwner" Background="#F5F6F8">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="RaceTitleText" FontSize="24" FontWeight="Bold" Foreground="#20232A"/>
            <TextBlock x:Name="RaceMetaText" FontSize="14" Foreground="#777777" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Leaderboard DataGrid -->
        <DataGrid x:Name="LeaderboardDataGrid" Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  Background="White"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0"
                  RowHeight="40"
                  FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Pos" Binding="{Binding PositionDisplay}" Width="80"/>
                <DataGridTextColumn Header="Racer ID" Binding="{Binding RacerId}" Width="120"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Time" Binding="{Binding FormattedFinishTime}" Width="160"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="CLOSE" Width="100" Height="40" Background="#20232A" Foreground="White" FontWeight="Bold" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
